ansible-navigator:
  execution-environment:
    enabled: false
  logging:
    file:
      .ansible/navigator/ansible-navigator.log
  playbook-artifact:
    save-as: "{playbook_dir}/.ansible/navigator/{playbook_name}-artifact-{time_stamp}.json"
