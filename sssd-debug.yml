---
- name: configure sssd debug_level
  ini_file:
    path: /etc/sssd/sssd.conf
    section: '{{item}}'
    option: debug_level
    value: 2
  notify: restart sssd
  loop:
    - domain/ipa.robots.org.uk
    - sssd
    - nss
    - pam
    - sudo
